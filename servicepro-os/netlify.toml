[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for any path so client-side
# React Router can handle deep links like /booking, /dashboard, /profile
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
